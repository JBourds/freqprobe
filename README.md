# Frequency Probe

Small Rust CLI program to either provide a real-time monitor of the frequency
each CPU core on your system runs or log the data to a CSV. See `cargo r -- --help`
for help with the command line arguments.
